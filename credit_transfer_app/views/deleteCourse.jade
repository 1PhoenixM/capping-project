extends ./wrapper.jade

block content
    h2 Delete Courses
    p
    form(name = 'deleteCourse', action='/admission/transfer/credits/app/deleteCourseAction',method='POST')
      table(style='width:100%', text-align='center', border='1')
        tr
          th Delete
          th School
          th Department
          th Course Name
          th Course Number
          th Credits
          th Description
          th Active
        each course in courses
          tr
            td
              input(type='checkbox', name='cid', value=course.sid+'|'+course.did+'|\''+course.coursenumber+'\'')
            td= course.schoolname
            td= course.departmentname
            td= course.coursename
            td= course.coursenumber
            td= course.credits
            td= course.description
            td= course.isactive
      input(type='submit' value='Submit')
